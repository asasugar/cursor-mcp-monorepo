{
  "name": "cursor-mcp-monorepo",
  "description": "A monorepo for Cursor MCP",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:todo-app": "pnpm --filter ./packages/todo-app build",
    "build:generate-docs": "pnpm --filter ./packages/generate-docs build"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0",
    "zod": "^3.24.2"
  }
}